node_modules/
dist/
build/
coverage/
.turbo/

.env
.env.*
!.env.example

*.keystore
*.jks
*.p12
*.mobileprovision

.DS_Store
*.log
.vscode/
.idea/

# The bot's SQLite database, written at runtime.
.leela.db
.leela.db-*

# Native projects are generated by `expo prebuild` and are not source.
apps/mobile/ios/
apps/mobile/android/
apps/mobile/.expo/
scripts/.mutants-undo.json

# Stryker's sandbox: a whole copy of this tree. `cleanTempDir: 'always'` removes
# it after a run that FINISHES, so what leaves it here is a run that was killed
# before that ran.
.stryker-tmp/

# Mimosa scanner state, not a fact about the game.
.mimosa/
