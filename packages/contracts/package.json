{
  "name": "@leela/contracts",
  "version": "1.0.0",
  "private": true,
  "description": "LeelaGame.sol, and a check that its board still matches the engine.",
  "type": "module",
  "main": "./src/verify.ts",
  "scripts": {
    "test": "vitest run --testTimeout=30000 --hookTimeout=60000",
    "typecheck": "tsc --noEmit",
    "typecheck:strict": "tsc --noEmit -p tsconfig.src.json"
  },
  "dependencies": {
    "@leela/engine": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "22.20.1",
    "typescript": "^5.6.3",
    "vitest": "^2.1.8"
  },
  "license": "MIT"
}
