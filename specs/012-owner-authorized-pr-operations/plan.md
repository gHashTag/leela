# Plan

1. Replace the three conflicting boundary blocks with one PR-only authority.
2. Record the measured Railway failure as amendment evidence.
3. Add a regression test that checks every agent-facing policy and a stale case.
4. Run the repository gate, open a PR to `main`, wait for checks, and merge it.
