# Plan

1. Build the complete static artifact in a Docker builder stage.
2. Serve it beside the existing API without changing API routing.
3. Allow the Railway origin to call its own authenticated API.
4. Test path mapping and image assembly.
5. Merge, deploy, update the Telegram menu button, and probe the live bundle.
